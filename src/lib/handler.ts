export const DndConsider = (e: any, items: any[]) => {
    items = e.detail.items;

    return items
}
export const DndFinalize = (e: any, items: any[]) => {
    items = e.detail.items;

    return items
}